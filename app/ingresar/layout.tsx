// app/ingresar/layout.tsx — Layout para login unificado y verificación de email
// Fuera de cualquier RouteGuard — es ruta pública
export default function IngresarLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
